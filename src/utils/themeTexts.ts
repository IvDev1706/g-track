import { TextConfig } from "../interfaces/config";

//titulos
export const TITLETEXT:TextConfig = {
    size: 32,
    weight: "bold",
    alignment: "center"
};

export const TITLETEXT2:TextConfig = {
    size: 22,
    weight: "bold",
    alignment: "center"
};

//texto normal
export const NORMALTEXT:TextConfig = {
    size: 18,
    weight: "normal",
    alignment: "left",
};

//texto de boton
export const BUTTONTEXT:TextConfig = {
    size: 14,
    weight: "normal",
    alignment: "center"
};
